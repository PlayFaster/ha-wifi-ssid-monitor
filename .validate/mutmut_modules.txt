# Modules worth mutating in this project, one glob per line.
#
# The sync appends these under `only_mutate=` in setup.cfg, indented. Do not
# indent them here and do not use commas — see the notes in setup.cfg.
#
# A module belongs here when its tests exercise real code. One whose tests mock
# its collaborator produces survivors no test could ever kill.
#
# WHY each module is on or off this list, what was tried and rejected, and what
# the candidates are:  .notes/test_pytest_issues/mutation_covered_not_covered.md
# Read it before proposing a change. Do not duplicate its reasoning here.

*/parse.py
*/diagnostics.py
*/health.py
*/coordinator.py
