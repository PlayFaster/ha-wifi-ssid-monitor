ruff==0.16.0
pytest-homeassistant-custom-component==0.13.354
pytest-cov==7.1.0
yamllint==1.38.0
check-jsonschema==0.37.4
codespell==2.4.3
# mutmut — mutation testing. DELIBERATELY NOT IN version_matrix.json, and that
# is not an oversight: it has no Home Assistant counterpart, no pre-commit hook,
# no Dockerfile entry and no CI use, so a matrix row would read "N/A" in four of
# six columns in a report whose value is that every row matters.
#
# The pin is still visible: the "Tests: Mutation Check" task prints the local
# and latest versions each run, which is the point at which anyone would decide
# to move it. Pinned for container reproducibility — it pulls textual, libcst
# and rich into a container that also runs Home Assistant.
mutmut==3.7.0
